var anos = prompt("digite um idade válida, anos") * 1;

var meses = prompt("digite um idade válida, meses") * 1;

var dias = prompt("digite um idade válida, dias") * 1;

var idade = 0;

    idade+=anos*365+meses*30+dias;

    console.log('idade em dias é: '+idade);
